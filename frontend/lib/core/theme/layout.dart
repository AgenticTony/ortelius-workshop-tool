/// Shared layout constants so screen widths and paddings stay consistent.
class Layout {
  Layout._();

  /// Max content width for form / centered screens (join, create, home).
  static const double contentMaxWidth = 480;

  /// Standard horizontal padding around screen content.
  static const double padding = 24.0;

  /// Standard gap between stacked sections.
  static const double sectionGap = 16.0;
}
