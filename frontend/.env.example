# Frontend environment config. Copy to .env and adjust.
# On web, the app auto-discovers the backend from the browser origin, so this
# is mainly used for non-web (simulator/desktop) dev.
API_BASE_URL=http://localhost:8000
